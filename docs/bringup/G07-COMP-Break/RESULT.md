# G7 门极驱动 / COMP / Break / PV_I 计量 — 验收记录

- **门禁状态：`IN_PROGRESS`（§9.6 1 A 通过；过流刹停+恢复已见；示波归档未齐）**
- 目标工程：`D:\work\mppt-charger-300w\Application`
- 门禁宏：`DRV_DEVICE_MIGRATION_GATE = 7U`
- 记录时间：2026-09-07；**同日下午追加 PV_I / 过流恢复**

> doc46 §9.5：保护链与 §9.6 PV_I 分维。保护链完整示波归档前，`GATE_COMP_ROUTE_VALIDATED` 保持 0。

---

## 1. 代码轨

| 项 | 状态 |
|---|---|
| `DRV_OPA_Init` / `DRV_COMP_Init` | GATE=7 已开 |
| COMP Break → ATMR | `DRV_PWM_Init` 绑 COMP0，AOE 关 |
| 刹停查询 | `DRV_PWM_IsBraked()` |
| 恢复 | `DRV_PWM_TryRecover()`（过流输入仍有效则不会误恢复） |
| 主循环 | 刹车检测 + 解除后自动尝试恢复 + 呼吸扫描 |
| `GATE_COMP_ROUTE_VALIDATED` | **0** |
| `GATE_ANALOG_CALIBRATED` | **0**（1 A 通路已通，拟合未写） |

---

## 2. 硬件保护链

### 2.1 已验证

| 日期 | 项 | 结果 |
|---|---|---|
| 2026-09-07 | COMP0 / COMP2 硬件跳变捕获 + 中断 | 通过 |
| 2026-09-07 下午 | 模拟过流 → PWM **刹停** | 通过 |
| 2026-09-07 下午 | 过流解除后 **恢复发波** | 通过（澄清：恢复路径已实现且台上可见） |
| 2026-09-07 下午 | 栅极驱动脉冲约 **12 V** | 通过（与 G6 联测） |
| 2026-09-07 晚间 | 互补 PWM+死区模式下过流 **仍能刹** | 通过（与 G6 联测） |

### 2.2 仍缺（正式保护链 `BOARD_PASS` / 进 G8 前建议）

- [ ] 四通道同屏：COMP 输入 / PB10(U6 EN) / GLC / **实际 Vgs**
- [ ] 关断延迟量化（相对 20 µs PWM 周期 / MOS SOA）
- [ ] **CPU halt 时硬件仍关 Vgs**（不依赖 ISR）
- [ ] 持续故障禁 ARM；解除后需再使能（与自动恢复策略对照写清）
- [ ] ACTIVE 期间 Break 锁存行为截图
- [ ] 极性与冻结表签字 → 才可 `GATE_COMP_ROUTE_VALIDATED=1`

---

## 3. §9.6 PV_I 计量

### 3.1 1 A 结果（2026-09-07 下午）— **通过**

| 状态 | seq 约 | code | I_cal | OPA_Vout |
|---|---|---|---|---|
| 1 A | 24257~41138 | 2134 / 2133 | 1108 / 1091 mA | 1719 / 1718 mV |
| 0 A（断开后） | ≥43013 | 2072 | 67 mA | 1669 mV |

```text
ΔV = 50.0 mV ≈ 理论 48.0 mV（1A×3mΩ×16）
Δcode = 62 LSB
未拟合误差 ≈ +10%~+11% vs 1000 mA 注入
```

判定：检流 → OPA×16 → PA8/ADC → 换算 **链路 100% 跑通**，极性正确。详细交叉见 `G05-.../RESULT.md`。

### 3.2 仍建议补

- [ ] 0.5 A / 2 A 低电流点（冻结增益更稳）
- [ ] 书面拟合回填 `drv_board` 增益/零点
- [ ] 高电流 4/6/9/12 A → G8 带载后

### 3.3 阻断条款

本板已证实 OPA_Vout 随电流联动 → **不适用**「PA8 非 OPA 输出」阻断。

---

## 4. 结论

**IN_PROGRESS**：§9.6 **1 A 通过**；过流 **刹停 + 恢复** 实板通过；完整保护链示波与标定签字未齐。  
`DRV_DEVICE_POWER_OUTPUT_ALLOWED` 保持 0。正式 G8 传能前补齐 §2.2。
