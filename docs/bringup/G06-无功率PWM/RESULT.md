# G6 PWM / 占空比 / 同步路径 — 验收记录

- **门禁状态：`IN_PROGRESS`（限幅与同步路径已测；产品单端 GHC 低待复核）**
- 目标工程：`D:\work\mppt-charger-300w\Application`
- 战役镜像：`DRV_DEVICE_MIGRATION_GATE = 7U`
- 记录：2026-09-07

> **红线**：产品基线为 **PA15 单端异步**（`CONFIG_PWM_SYNC_BOOST_ENABLE=0`，PA14 恒低、无死区）。  
> 互补+死区仅证明同步路径可用，**不替代**单端红线验收。

---

## 1. 代码轨

| 项 | 状态 |
|---|---|
| `DRV_PWM_SetDuty` 限幅 900‰ | 已编入 |
| 呼吸扫描 | bring-up `main.c` |
| 同步/单端切换 | `CONFIG_PWM_SYNC_BOOST_ENABLE` + 死区 `DRV_PWM_DEADTIME_NS` |

---

## 2. 实板证据

### 2.1 限幅与呼吸（下午）

| 项 | 结果 |
|---|---|
| 栅极驱动 | ≈12 V 脉冲 |
| 占空比 | 可见 0%~90% |
| 超限 | 钳位 90%，`[CLAMP 90%]` |

### 2.2 同步路径 + 限幅/过流仍有效（晚间）

| 项 | 结果 |
|---|---|
| 互补 PWM + 死区 | **台架完成** |
| 90% 限幅 | **仍有效** |
| 过流刹停 | **仍有效**（交叉见 G07） |

勾选：

- [x] 动态占空比 / 90% 限幅
- [x] 互补 + 死区路径
- [x] 同步模式下过流仍能刹

仍缺（产品路径）：

- [ ] **`SYNC=0` 单端**：PA15 有波、**PA14 全程 LOW**
- [ ] 50 kHz 周期量化归档（可选）
- [ ] ≥2 h 空跑（可后置）

---

## 3. 结论

同步路径与 90% 限幅已实板通过。进 G8 前请切回单端并确认 GHC 低。功率总门保持 0。
