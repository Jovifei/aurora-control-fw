# 21｜合并后维护说明

后续修改 `app/` 或 `driver/` 下 C/H 文件时，应继续保留统一的文件头和函数头格式，并执行：

```bash
python tools/check_text_encoding.py
python tools/check_comment_style.py
python tools/run_checks.py
```

新增模块时应同步更新本目录对应模块文档和 `14-全工程函数调用索引.md`。涉及 PWM、Relay、Flash、COMP 或功率门禁的修改，不得仅以 Host 测试代替 Keil 与实板证据。
